{
  "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "resourceBaseName": {
      "value": "bot-${{RESOURCE_SUFFIX}}-${{TEAMSFX_ENV}}"
    },
    "botAadAppClientId": {
      "value": "${{BOT_ID}}"
    },
    "botDisplayName": {
      "value": "${{APP_DISPLAY_NAME}}"
    },
    "botAadAppClientSecret": {
      "value": "${{SECRET_BOT_PASSWORD}}"
    },
    "webAppSKU": {
      "value": "B1"
    },
    "graphAadAppClientId": {
      "value": "${{GRAPH_AAD_APP_ID}}"
    },
    "graphAadAppClientSecret": {
      "value": "${{SECRET_GRAPH_AAD_APP_CLIENT_SECRET}}"
    },
    "connectionName": {
      "value": "${{CONNECTION_NAME}}"
    },
    "spoHostName": {
      "value": "${{SPO_HOSTNAME}}"
    },
    "spoSiteUrl": {
      "value": "${{SPO_SITE_URL}}"
    }
  }
}